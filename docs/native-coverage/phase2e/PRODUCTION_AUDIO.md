# Production DTS / TrueHD / MLP

Amendment 24 is applied. `Dts`, `TrueHd`, `Mlp` follow `ProRes4444`, preserving existing ordinals. Production configuration selects a real libavcodec plan with raw extradata; Apple format-tag/cookie handling remains unchanged. The worker reports channel roles from its first decoded frame before any PCM is published. A coalesced owner wake drains asynchronous output. The source alone owns retained-window clipping; libavcodec automatic skip trimming is not applied a second time.

The admitted profile is 48 kHz DTS core with 512-sample access units and 48 kHz TrueHD/MLP with 40-sample ordinals. DTS HD-MA is not covered by a retained production specimen and remains refused. TrueHD/MLP validate major sync and wrapping input timing, including 10-byte silent MLP packets. Coarse Matroska ticks must bracket the exact sample ordinal with their adjacent floor/ceiling ticks. Origin is zero. Forward/backward seeks replay from the original preceding major sync: bounded memory, but O(target) preroll latency; nearest-major-sync indexing is not claimed.

The production converter proof synthesizes dca, truehd and mlp encodings in Matroska beside video, in stereo and 5.1, and checks four targets per layout/codec (24 cases). Every 2-second retained window has 96,000 samples and equals the video duration exactly. DTS produces 96,256 decoded frames and clips 256 once; TrueHD and MLP produce 96,000. Targets 1, 1/7 and 1999/1000 land at ceil(T×48000)/48000. PCM has zero-frame alignment to independent ffmpeg decode, maximum error below 2e-6 and RMS below 2e-7. Six-channel impulse roles are checked through the production downmix.

Three quiet app runs draw 50/50 video frames with 96,000 rendered samples each, zero late/superseded video, no native failure and zero clock-advanced underruns. TrueHD/MLP report end-of-stream underrun callbacks without advancing their clocks; those callbacks are not hidden. The forward/backward/near-EOF app seek sequence has no native failure. Exact sample landing is established by the production PCM probe, not floating-point GUI telemetry.

Existing frozen Apple converter/session test files and the session header remain byte-identical to HEAD. New tests are separate files. The production source/converter/session temporary revert refuses the new identities; byte-identical restoration passes all 24 PCM cases and the three GUI runs. See audio-production.json, audio-session.json, audio-revert-proof.json, frozen-invariants.json and amendment24-applied.md.
