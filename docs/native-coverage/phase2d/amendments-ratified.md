# Phase 2d ratification ledger

AMENDMENT 24 (production software-audio identity and converter contract), RATIFIED by the maintainer in the phase-2d directive. Append `Dts`, `TrueHd`, `Mlp` immediately after `ProRes4444` in MediaCodec, preserving every existing ordinal and signature. DTS core and HD-MA share the family; profile admission requires its own retained specimen. Select backend and DecodePlan at configuration; Apple rows retain format-tag/magic-cookie checks, libavcodec rows use raw extradata and real implementation identity. Replace AudioMagicCookie-only ingress and the four-field backend aggregate with representation-aware ingress. Establish multichannel roles from the FIRST DECODED FRAME before publication. Select lead-in/reset/deficit/tail semantics by implementation and stream facts, preserving SKIP_MANUAL: the source owns the exact retained window and the converter only clips to it. Never apply both automatic decoder trimming and WAM trimming. Preserve exact first-sample and ceiling checks. TrueHD/MLP use 40-sample ordinals at 48 kHz with seek planning from a preceding major sync and proof that the first published frame is ceil(T×R). Existing frozen Apple converter/session TEST EXPECTATIONS remain byte-identical; new software integration tests belong in separate files.

Application status at ratification: no frozen line touched. Exact before/after patches will be appended here for each applied change; authorization alone is not acceptance.

AMENDMENT 25 (decoder-private admission, separate from presentation), RATIFIED by the maintainer in the phase-2d directive. In native_surface_budget.hpp's software block, replace the blanket 1920x1080 area refusal with admission by a derived byte reservation per codec, bit depth, chroma format, coded alignment and maximum reference count. Account separately for packet storage, conversion scratch, decoder-private references/scratch and worker reservations, retiring them on cancellation. Preserve the presentation 10-surface/384 MiB limits. State the combined software audio+video worker reservation against the 16-worker process limit. Derive an upper bound from source facts or an enforceable allocator reservation; the 32-frame measurements are evidence, never a peak rounded into a ceiling.

Application status at ratification: no frozen line touched. Exact before/after patches will be appended here for each applied change; authorization alone is not acceptance.
